import React, { Component } from 'react';
import Nav from '../../components/Nav/Nav';

class Post extends Component {
   render() {
      return (
         <div>
            < Nav />
            Post
         </div>
      );
   }
}

export default Post;